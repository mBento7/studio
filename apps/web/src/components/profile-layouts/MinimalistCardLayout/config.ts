export type MinimalistCardLayoutConfig = {
  showPortfolio?: boolean;
  themeColor?: string;
};

export const defaultMinimalistCardLayoutConfig: MinimalistCardLayoutConfig = {
  showPortfolio: false,
  themeColor: '#0f172a',
}; 