export type FreeProfileLayoutConfig = {
  showPortfolio?: boolean;
  showSocialLinks?: boolean;
  themeColor?: string;
};

export const defaultFreeProfileLayoutConfig: FreeProfileLayoutConfig = {
  showPortfolio: true,
  showSocialLinks: true,
  themeColor: '#f59e42',
}; 