export type ModernProfileLayoutConfig = {
  showPortfolio?: boolean;
  showSocialLinks?: boolean;
  themeColor?: string;
};

export const defaultModernProfileLayoutConfig: ModernProfileLayoutConfig = {
  showPortfolio: true,
  showSocialLinks: true,
  themeColor: '#f59e42',
}; 